import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Services | Workflow Automation, Smart Agents & Custom Solutions | Origin Dev Labs",
  description: "Workflow automation, intelligent agents, smart assistants, business intelligence, and custom solutions. See how Origin Dev Labs can automate your operations.",
}

export default function ServicesLayout({ children }: { children: React.ReactNode }) {
  return children
}